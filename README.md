# 🌿 HealthyBot — Telegram-бот для отслеживания состояния здоровья

## Состав проекта

── bot.py: Инициализация бота, диспетчера, middleware

── config.py: Загрузка переменных окружения (.env)

── handlers.py: Все обработчики команд и FSM-диалогов

── states.py: FSM-состояния (профиль, вода, тренировки)

── middlewares.py: Middleware для логирования сообщений и callback-запросов

── requirements.txt: Зависимости проекта

── runtime.txt: Версия Python для Render.com

── Procfile: Инструкция запуска для Render

── Deploy1: Скриншот деплоя на Render.com 

── Screenshots: Скриншоты работы бота 


## Основные обработчики команд

- **`/start`** — приветствие и начало работы.  
- **`/help`** — выводит список всех доступных команд.  
- **`/set_profile`** — настройка профиля через FSM-диалог: вес → рост → возраст → активность → город.  
- **`/log_water`** — запись выпитой воды в миллилитрах; обновляет прогресс по норме.  
- **`/log_food`** — поиск продукта через OpenFoodFacts API, ввод граммов, расчёт калорий.  
- **`/log_workout`** — выбор типа тренировки из кнопок, ввод длительности, расчёт сожжённых калорий и дополнительной воды.  
- **`/check_progress`** — вывод текущего состояния: сколько воды выпито, калорий потреблено и сожжено.  
- **`/graph`** — построение и отправка графиков прогресса по воде и калориям (столбчатые диаграммы).

## Технологии

Проект написан на Python 3.11 с использованием фреймворка aiogram 3.13. 

Для HTTP-запросов к API применяется aiohttp. 

Данные о продуктах берутся из открытого API OpenFoodFacts, погода — из OpenWeatherMap. 

Графики строятся с помощью matplotlib. 


## Деплой

Бот задеплоен и успешно работает в облаке на [Render.com](https://render.com).  



